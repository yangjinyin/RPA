## 一、核心数据结构

### 1. **trx_t** - 事务对象 ([trx0trx.h:680](vscode-webview://0rladas7ugatl6fj3c8b9mpi686a162sd2pjfvdru1oart907b2q/storage/innobase/include/trx0trx.h#L680))

这是事务系统最核心的数据结构，包含：

**关键字段：**

- `trx_id_t id` - 事务ID（写事务才分配）
- `trx_id_t no` - 事务序列号（提交时分配，用于MVCC）
- `std::atomic<trx_state_t> state` - 事务状态
- `ReadView *read_view` - 一致性读视图
- `trx_lock_t lock` - 锁相关信息
- `trx_rsegs_t rsegs` - Rollback segments（包含undo日志）
- `isolation_level_t isolation_level` - 隔离级别
- `undo_no_t undo_no` - 下一个undo记录编号

**状态枚举 ([trx0types.h:80](vscode-webview://0rladas7ugatl6fj3c8b9mpi686a162sd2pjfvdru1oart907b2q/storage/innobase/include/trx0types.h#L80))：**

```cpp
TRX_STATE_NOT_STARTED          // 未启动
TRX_STATE_FORCED_ROLLBACK      // 强制回滚
TRX_STATE_ACTIVE               // 活跃
TRX_STATE_PREPARED             // 准备阶段（XA）
TRX_STATE_COMMITTED_IN_MEMORY  // 内存已提交
```

### 2. **trx_lock_t** - 锁管理结构 ([trx0trx.h:406](vscode-webview://0rladas7ugatl6fj3c8b9mpi686a162sd2pjfvdru1oart907b2q/storage/innobase/include/trx0trx.h#L406))

```cpp
struct trx_lock_t {
    ulint n_active_thrs;                    // 活跃线程数
    trx_que_t que_state;                    // 查询执行状态
    std::atomic<lock_t *> wait_lock;        // 正在等待的锁
    std::atomic<trx_t *> blocking_trx;      // 阻塞当前事务的事务
    trx_lock_list_t trx_locks;              // 持有的锁列表
    std::atomic<ulint> n_rec_locks;         // 记录锁数量
    mem_heap_t *lock_heap;                  // 锁内存堆
};
```

### 3. **trx_rsegs_t** - Rollback Segments ([trx0trx.h:662](vscode-webview://0rladas7ugatl6fj3c8b9mpi686a162sd2pjfvdru1oart907b2q/storage/innobase/include/trx0trx.h#L662))

```cpp
struct trx_rsegs_t {
    trx_undo_ptr_t m_redo;    // 持久化rollback segment（需要恢复）
    trx_undo_ptr_t m_noredo;  // 临时表rollback segment（无需恢复）
};

struct trx_undo_ptr_t {
    trx_rseg_t *rseg;          // rollback segment指针
    trx_undo_t *insert_undo;   // insert undo log
    trx_undo_t *update_undo;   // update undo log
};
```

### 4. **trx_sys_t** - 事务系统全局结构 ([trx0sys.h:454](vscode-webview://0rladas7ugatl6fj3c8b9mpi686a162sd2pjfvdru1oart907b2q/storage/innobase/include/trx0sys.h#L454))

```cpp
struct trx_sys_t {
 MVCC *mvcc; 	//管理所有 ReadView，协调一致性读
 Rsegs rsegs;   //持久化表空间的回滚段
 Rsegs tmp_rsegs; //临时表空间的回滚段
    
 std::atomic<uint64_t> rseg_history_len;//TRX_RSEG_HISTORY链表的长度(已提交事务的undo日志),purge线程根据此值来清理
 
 std::atomic<trx_id_t> next_trx_id_or_no; //分配下一个trx_id 或者trx_no
 trx_id_t rw_max_trx_no;  //最大的的trx_no
 trx_ids_t rw_trx_ids; //活跃事务号列表
 UT_LIST_BASE_NODE_T(trx_t, no_list) serialisation_list;//已分配 trx->no 但尚未完成提交的事务列表
 std::atomic<trx_id_t> serialisation_min_trx_no; // Purge 线程不能清理 >= serialisation_min_trx_no 的 undo
 UT_LIST_BASE_NODE_T(trx_t, mysql_trx_list) mysql_trx_list;//所有事务号列表
 
}
```

## 二、事务生命周期关键函数

### 1. **事务启动**

#### 核心函数调用链：

```
trx_start_if_not_started() 
  └─> trx_start_if_not_started_low()     [trx0trx.cc:3323]
      └─> trx_start_low()                 [trx0trx.cc:1245] ⭐核心
```

**trx_start_low() 关键逻辑：**

1. **状态检查** - 确保事务未启动

2. **属性初始化** - 设置隔离级别、只读标志、启动时间

3. 分配事务ID

   （写事务）：

   ```cpp
   trx->id = trx_sys_allocate_trx_id();  // 从全局计数器分配
   ```

4. 分配Rollback Segment

   ```cpp
   trx_assign_rseg_durable(trx);  // 持久化段
   trx_assign_rseg_temp(trx);      // 临时表段
   ```
   
5. 加入全局列表

   ```cpp
   trx_sys->rw_trx_ids.push_back(trx->id);
   UT_LIST_ADD_FIRST(trx_sys->rw_trx_list, trx);
   ```

### 2. **事务提交**

#### 核心函数调用链：

```
trx_commit_for_mysql()                   [trx0trx.cc:2384]
  └─> trx_commit()                       [trx0trx.cc:2198]
      └─> trx_commit_low()               [trx0trx.cc:2106] ⭐核心
          ├─> trx_write_serialisation_history()  [行1512]
          └─> trx_commit_in_memory()             [行1904]
```

**trx_write_serialisation_history() - 序列化提交历史 ([trx0trx.cc:1512](vscode-webview://0rladas7ugatl6fj3c8b9mpi686a162sd2pjfvdru1oart907b2q/storage/innobase/trx/trx0trx.cc#L1512))：**

1. 获取rollback segment互斥锁

2. 分配事务序列号

   （trx->no）：

   ```cpp
   trx_serialisation_number_get(trx, ...);  // 全局递增序列号
   ```

3. 设置undo状态：

   - Insert undo → `TRX_UNDO_CACHED`（可复用）
   - Update undo → `TRX_UNDO_TO_PURGE`（加入purge队列）

4. 更新binlog位置

5. 写入redo log

**trx_commit_in_memory() - 内存提交 ([trx0trx.cc:1904](vscode-webview://0rladas7ugatl6fj3c8b9mpi686a162sd2pjfvdru1oart907b2q/storage/innobase/trx/trx0trx.cc#L1904))：**

1. 释放所有锁

   ```cpp
   lock_trx_release_locks(trx);  // 记录锁 + 表锁
   ```
   
2. 关闭ReadView

   ```cpp
   trx_sys->mvcc->view_close(trx->read_view, false);
   ```
   
3. **清理Insert Undo**（立即truncate）

4. **状态转换**：`TRX_STATE_COMMITTED_IN_MEMORY`

5. **从全局列表移除**

### 3. **事务回滚**

#### 核心函数调用链：



```
trx_rollback_for_mysql()                 [trx0roll.cc:268]
  └─> trx_rollback_low()                 [trx0roll.cc:180]
      └─> trx_rollback_to_savepoint_low() [trx0roll.cc:78] ⭐核心
          └─> trx_rollback_finish()       [trx0roll.cc:1042]
```

**trx_rollback_to_savepoint_low() 关键步骤：**

1. 创建回滚执行图

   ```cpp
   roll_node = roll_node_create(heap);
   if (savept != nullptr) {
       roll_node->partial = true;  // 部分回滚
   }
   ```
   
2. 执行Undo回放

   ```cpp
   que_run_threads(thr);  // 读取undo记录并反向应用
   ```
   
3. 回放逻辑

   （从新到旧）：

   - 读取undo记录
   - 根据undo type执行反向操作：
     - INSERT → DELETE
     - DELETE → INSERT（恢复旧版本）
     - UPDATE → UPDATE（恢复旧值）

**Savepoint回滚：**

```cpp
trx_rollback_to_savepoint_for_mysql()    [trx0roll.cc:431]
trx_rollback_last_sql_stat_for_mysql()   [trx0roll.cc:283] // 回滚最后一条SQL
```

## 三、锁管理核心逻辑

### 关键函数

**锁获取：**

- `lock_rec_lock()` [lock0lock.cc:1935](vscode-webview://0rladas7ugatl6fj3c8b9mpi686a162sd2pjfvdru1oart907b2q/storage/innobase/lock/lock0lock.cc#L1935) - 记录锁
- `lock_table()` - 表锁
- `lock_rec_add_to_queue()` - 加入锁等待队列

**锁释放：**

- ```
  lock_trx_release_locks(trx)
  ```

  \- 释放所有锁

  ```cpp
  locksys::try_release_all_locks(trx);
  trx->lock.n_rec_locks.store(0);
  mem_heap_empty(trx->lock.lock_heap);  // 批量释放锁内存
  ```

**死锁检测：**

- `lock_deadlock_check_and_resolve()` - 死锁检测
- `lock_has_to_wait()` - 判断锁冲突

### 锁存储优化

- **Record Lock Bitmap**：同一页面的记录锁用bitmap压缩存储
- **隐式锁**：延迟到冲突时才转换为显式锁
- **Lock Heap**：统一内存池，批量释放

## 四、Undo日志管理

### 核心函数

**分配与创建：**

```cpp
trx_undo_assign_undo()           [trx0undo.cc:1687] // 分配undo段
trx_undo_page_report_insert()    // 记录INSERT undo
trx_undo_page_report_modify()    // 记录UPDATE/DELETE undo
```

**状态管理：**

```cpp
trx_undo_set_state_at_finish()   [trx0undo.cc:1798] // 提交时设置状态
trx_undo_set_state_at_prepare()  // XA PREPARE时设置状态
```

**清理：**

```cpp
trx_undo_insert_cleanup()        [trx0undo.cc:1956] // Insert undo立即清理
trx_undo_update_cleanup()        // Update undo加入purge队列
```

### Undo记录格式

每条Undo记录包含：

- **Type**：TRX_UNDO_INSERT_REC / TRX_UNDO_UPD_EXIST_REC / TRX_UNDO_DEL_MARK_REC
- **undo_no**：事务内序号
- **table_id**：表ID
- **主键值**：用于定位记录
- **旧值**：UPDATE前的列值（变化列）

### Roll Pointer结构

每个记录的隐藏列 `DB_ROLL_PTR` (7字节)：

- **rseg_id** (7 bits)：rollback segment ID
- **page_no** (32 bits)：undo页号
- **offset** (16 bits)：页内偏移

## 五、MVCC实现

### ReadView结构

```cpp
class ReadView {
    trx_id_t m_low_limit_id;     // 最大事务ID + 1（不可见）
    trx_id_t m_up_limit_id;      // 最小活跃事务ID（可见）
    trx_ids_t m_ids;             // 创建时活跃的事务ID数组
    trx_id_t m_creator_trx_id;   // 创建者事务ID
};
```

### 可见性判断

**核心逻辑：**

```cpp
bool changes_visible(trx_id_t id) {
    if (id < m_up_limit_id) return true;      // 早于所有活跃事务
    if (id >= m_low_limit_id) return false;   // 晚于ReadView创建
    if (id == m_creator_trx_id) return true;  // 自己的修改
    return !m_ids.contains(id);               // 不在活跃列表中
}
```

**版本链回溯：**

1. 读取记录的 `DB_TRX_ID` 和 `DB_ROLL_PTR`
2. 判断该版本是否可见
3. 不可见则通过roll_ptr找到undo记录
4. 从undo记录构造旧版本
5. 重复2-4直到找到可见版本

### ReadView分配时机

```cpp
trx_assign_read_view(trx)  [trx0trx.cc:2260]
```

- **RC隔离级别**：每条语句创建新ReadView
- **RR隔离级别**：事务内首次读创建，之后复用

## 六、XA事务（两阶段提交）

### 状态转换

```
NOT_STARTED → ACTIVE → PREPARED → COMMITTED
```

### 关键函数

**PREPARE阶段：**

```cpp
trx_prepare_for_mysql()  [trx0trx.cc:2316]
  └─> 设置undo状态为 TRX_UNDO_PREPARED
  └─> 持久化XA事务状态
  └─> trx->state = TRX_STATE_PREPARED
```

**COMMIT/ROLLBACK阶段：**

- 从PREPARED状态恢复到ACTIVE
- 执行正常的提交或回滚流程

### XA恢复

启动时扫描undo日志，恢复PREPARED状态的事务：

```cpp
trx_lists_init_at_db_start()  // 重建事务列表
trx_resurrect_locks()         // 恢复锁
```

## 七、性能优化设计

### 1. **自动提交非锁定只读事务（AC-NL-RO）**

- 不分配事务ID
- 不加入rw_trx_list
- 状态切换无需mutex
- 极大减少只读查询开销

### 2. **Group Commit**

```cpp
trx->flush_log_later = true;  // 延迟刷盘
```

多个事务批量刷盘，减少fsync次数

### 3. **Undo日志分离**

- **Insert Undo**：提交后立即truncate（无需purge）
- **Update Undo**：加入purge队列异步清理

### 4. **事务分片（Sharding）**

```cpp
Trx_shard shards[256];  // 256个分片
```

减少全局trx_sys->mutex竞争

### 5. **Lock Pool**

预分配锁对象池，避免频繁malloc/free

## 八、完整流程图



```c++
┌─────────────────────────────────────────────────────────────┐
│                    事务完整生命周期                            │
└─────────────────────────────────────────────────────────────┘

1. BEGIN / 首个DML
   ├─> trx_start_low()
   ├─> 分配trx_id（写事务）
   ├─> 分配rollback segment
   └─> state = TRX_STATE_ACTIVE

2. 执行DML
   ├─> 首次修改：trx_undo_assign_undo()
   ├─> 获取锁：lock_rec_lock()
   ├─> 写undo记录
   └─> 修改数据页（设置trx_id和roll_ptr）

3. SELECT（一致性读）
   ├─> 首次：trx_assign_read_view()
   ├─> 可见性判断
   └─> 需要时通过roll_ptr构造旧版本

4. COMMIT
   ├─> trx_write_serialisation_history()
   │   ├─> 分配trx->no
   │   ├─> insert undo → CACHED
   │   └─> update undo → TO_PURGE
   ├─> mtr_commit()（持久化）
   ├─> trx_commit_in_memory()
   │   ├─> lock_trx_release_locks()
   │   ├─> 关闭ReadView
   │   └─> state = COMMITTED_IN_MEMORY
   └─> 刷日志（根据innodb_flush_log_at_trx_commit）

5. ROLLBACK
   ├─> trx_rollback_to_savepoint_low(NULL)
   ├─> 读取undo记录反向执行
   ├─> lock_trx_release_locks()
   └─> state = NOT_STARTED
```

# 9.核心结构体

***trx_t:\*** 整个结构体每个连接持有一个，也就是在创建连接后执行第一个事务开始，整个结构体就被初始化了，后续这个连接的所有事务一直复用里面的数据结构，直到这个连接断开。同时，事务启动后，就会把这个结构体加入到全局事务链表中(`trx_sys->mysql_trx_list`)，如果是读写事务，还会加入到全局读写事务链表中(`trx_sys->rw_trx_list`)。在事务提交的时候，还会加入到全局提交事务链表中(`trx_sys->trx_serial_list`)。state字段记录了事务四种状态:`TRX_STATE_NOT_STARTED`, `TRX_STATE_ACTIVE`, `TRX_STATE_PREPARED`, `TRX_STATE_COMMITTED_IN_MEMORY`。 这里有两个字段值得区分一下，分别是id和no字段。id是在事务刚创建的时候分配的(只读事务永远为0，读写事务通过一个全局id产生器产生，非0)，目的就是为了区分不同的事务(只读事务通过指针地址来区分)，而no字段是在事务提交前，通过同一个全局id生产器产生的，主要目的是为了确定事务提交的顺序，保证加入到`history list`中的update undo有序，方便purge线程清理。 此外，trx_t结构体中还有自己的read_view用来表示当前事务的可见范围。分配的insert undo slot和update undo slot。如果是只读事务，read_only也会被标记为true。

```c++
struct trx_t {
  enum isolation_level_t {
    READ_UNCOMMITTED，
    READ_COMMITTED,
    REPEATABLE_READ,
    SERIALIZABLE
  };
  trx_id_t id; //事务id
  trx_id_t no; //事务序列化号，提交前分配
  std::atomic<trx_state_t> state;//事务状态 支持的状态转换包括：NOT_STARTED → ACTIVE → COMMITTED → NOT_STARTED
  ReadView *read_view; //一致性读版本
  trx_lock_t lock;  //锁相关信息  1. wait_lock: 正在等待的锁 2.blocking_trx: 持有冲突锁的事务 3.trx_locks: 事务持有的锁列表...
  trx_rsegs_t rseg; //回滚段
  undo_no_t undo_no;//下一个 undo 日志记录号
  trx_savept_t last_sql_stat_start; //最后一条 SQL 语句开始时的 undo_no
 ...
}
```

| 维度                 | trx->id                   | trx->no                                                      |
| -------------------- | ------------------------- | ------------------------------------------------------------ |
| **含义**             | 事务标识符                | 事务序列化号（提交序号）                                     |
| **分配时机**         | 首次修改数据时            | 提交前                                                       |
| **初始值**           | 0                         | TRX_ID_MAX                                                   |
| **是否所有事务都有** | 否（只有读写事务）        | 是（包括只读）                                               |
| **用途**             | 版本标记、MVCC            | Purge、事务提交顺序(Purge 线程只能清理<br />`trx->no < m_low_limit_no` 的 undo 日志) |
| **记录在数据行**     | 是（DB_TRX_ID）           | 否                                                           |
| **分配函数**         | trx_sys_allocate_trx_id() | trx_sys_allocate_trx_no()                                    |



***trx_sys_t:\*** 这个结构体用来维护系统的事务信息，全局只有一个，在数据库启动的时候初始化。比较重要的字段有：max_trx_id，这个字段表示系统当前还未分配的最小事务id，如果有一个新的事务，直接把这个值作为新事务的id，然后这个字段递增即可。descriptors，这个是一个数组，里面存放着当前所有活跃的读写事务id，当需要开启一个readview的时候，就从这个字段里面拷贝一份，用来判断记录的对事务的可见性。rw_trx_list，这个主要是用来存放当前系统的所有读写事务，包括活跃的和已经提交的事务。按照事务id排序，此外，奔溃恢复后产生的事务和系统的事务也放在上面。mysql_trx_list，这里面存放所有用户创建的事务，系统的事务和奔溃恢复后的事务不会在这个链表上，但是这个链表上可能会有还没开始的用户事务。trx_serial_list，按照事务no(trx_t->no)排序的已经提交的事务。rseg_array，这个指向系统所有可以用的回滚段(`undo segments`)，当某个事务需要回滚段的时候，就从这里分配。rseg_history_len， 所有提交事务的update undo的长度，也就是上文提到的历史链表的长度，具体的update undo链表是存放在这个undo log中以文件指针的形式管理起来。view_list，这个是系统当前所有的readview, 所有开启的readview的事务都会把自己的readview放在这个上面，按照事务no排序。

```c++
struct trx_sys_t {
 MVCC *mvcc; 	//管理所有 ReadView，协调一致性读
 Rsegs rsegs;   //持久化表空间的回滚段
 Rsegs tmp_rsegs; //临时表空间的回滚段
    
 std::atomic<uint64_t> rseg_history_len;//TRX_RSEG_HISTORY链表的长度(已提交事务的undo日志),purge线程根据此值来清理
 
 std::atomic<trx_id_t> next_trx_id_or_no; //分配下一个trx_id 或者trx_no
 trx_id_t rw_max_trx_no;  //最大的的trx_no
 trx_ids_t rw_trx_ids; //活跃事务号列表
 UT_LIST_BASE_NODE_T(trx_t, no_list) serialisation_list;//已分配 trx->no 但尚未完成提交的事务列表
 std::atomic<trx_id_t> serialisation_min_trx_no; // Purge 线程不能清理 >= serialisation_min_trx_no 的 undo
 UT_LIST_BASE_NODE_T(trx_t, mysql_trx_list) mysql_trx_list;//所有事务号列表
 
}
```



***trx_purge_t:\*** Purge线程使用的结构体，全局只有一个，在系统启动的时候初始化。view，是一个readview，Purge线程不会尝试删除所有大于view->low_limit_no的undolog。limit，所有小于这个值的undolog都可以被truncate掉，因为标记的日志已经被删除且不需要用他们构建之前的历史版本。此外，还有rseg，page_no, offset，hdr_page_no, hdr_offset这些字段，主要用来保存最后一个还未被purge的undolog。

***read_view_t:\*** InnDB为了判断某条记录是否对当前事务可见，需要对此记录进行可见性判断，这个结构体就是用来辅助判断的。每个连接都的trx_t里面都有一个readview，在事务需要一致性的读时候(不同隔离级别不同)，会被初始化，在读结束的时候会释放(缓存)。low_limit_no，这个主要是给purge线程用，readview创建的时候，会把当前最小的提交事务id赋值给low_limit_no，这样Purge线程就可以把所有已经提交的事务的undo日志给删除。low_limit_id, 所有大于等于此值的记录都不应该被此readview看到，可以理解为high water mark。up_limit_id, 所有小于此值的记录都应该被此readview看到，可以理解为low water mark。descriptors, 这是一个数组，里面存了readview创建时候所有全局读写事务的id，除了事务自己做的变更外，此readview应该看不到descriptors中事务所做的变更。view_list，每个readview都会被加入到trx_sys中的全局readview链表中。

***trx_id_t:\*** 每个读写事务都会通过全局id产生器产生一个id，只读事务的事务id为0，只有当其切换为读写事务时候再分配事务id。为了保证在任何情况下(包括数据库不断异常恢复)，事务id都不重复，InnoDB的全局id产生器每分配256(`TRX_SYS_TRX_ID_WRITE_MARGIN`)个事务id，就会把当前的max_trx_id持久化到ibdata的系统页上面。此外，每次数据库重启，都从系统页上读取，然后加上256(`TRX_SYS_TRX_ID_WRITE_MARGIN`)。

***trx_rseg_t:\*** undo segment内存中的结构体。每个undo segment都对应一个。update_undo_list表示已经被分配出去的正在使用的update undo链表，insert_undo_list表示已经被分配出去的正在使用的insert undo链表。update_undo_cached和insert_undo_cached表示缓存起来的undo链表，主要为了快速使用。last_page_no, last_offset, last_trx_no, last_del_marks表示这个undo segment中最后没有被Purge的undolog。

